/*eslint-disable*/
import React from 'react';
import 'antd/dist/antd.css';  // or 'antd/dist/antd.less'
import farm from './farm.jpeg'
const MicroFbPostContent = () => (
    <div>
        <div className="micro-fb-post-content-text">
            Post Content Text. Long content generates automatically. VSCode is now taking up the web development in terms of community preferred editor especially when it comes to the development that involved type system (Typescript / Flow). It has rich built in support for types.
            When developing the react application using Vscode, people may find very difficult who where used to emmets. Vscode comes with built in support for emmet for HTML. But,
        </div>
        <div>
            <img src={farm} width="100%" height="50%" alt="image" />
        </div>
    </div>
      );

export default MicroFbPostContent;
/*eslint-disable*/
